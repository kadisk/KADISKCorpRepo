{

    "params" : [ "entryPort", "targetHost" ],
    "services":[
        {
            "namespace": "@@/transit-proxy-service",
            "dependency": "@/kadisk-proxy.service/services/TransitProxyService",
            "params":{
                "entryPort":"{{entryPort}}",
                "targetHost": "{{targetHost}}"
            }
        }
    ]
}